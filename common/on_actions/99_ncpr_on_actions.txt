# Called during empire initialization when starting a new game if has_secondary_species game rule is true.
# Called before empire_init_create_capital_pops.
# scope: planet
# from: founder species
# fromfrom: secondary species (if exists)
empire_init_capital_planet = {
	events = {
		ncpr_game_start.01
	}
}

# Called when a pop is purged, to (potentially) convert it to a necrophage
# This has a limit for the purging empire having the necropurifier origin, so
# it is mutually exclusive with necrophage and shouldn't create duplicate triggers
on_pop_purged = {
	events = {
		ncpr_purge.01
	}
}